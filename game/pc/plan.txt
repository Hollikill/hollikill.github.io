Listable:
    c: constructor
        name, category
    f: add multiplier
        category, id
        // eg. "income", "job_pay"
        // inheriting classes will override this method, switch for added categories, then super.addmultiplier()
    f: get requirements
        // if none exists, creates one
    f: get category
    f: set category
    prototype f: update listing
    prototype f: tick

Levelable:
    c: constructor
        name, category, xp_base, xp_scaling={...}
    f: set active
        boolean

Job sub Levelable:
    c: constructor
        name, category, xp_base, earn_base, xp_scaling={...}, earn_scaling={...}
    override f: update listing
        // if active job, set the income in player UI

Skill sub Levelable:
    c: constructor
        name, category, xp_base, components {}, xp_scaling={...}

Buyable sub Listable:
    c: constructor
        name, category, cost, components {}
        // auto-sets requiremnts to money:{100x the cost value}
    f: get singleton
    f: set singleton