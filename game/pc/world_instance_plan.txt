what needs to be created on world instance gen:
- categories added to list
- jobs added to list
- skills added to list
- items added to list
///
- subscribe all multipliers

what needs to be stored per world instance:
- jobs list
- skills list
- items list

what needs to be added to html page:
1. fresh world basic holder from template
2. job/skill/item listings

TODO
- add maxlevel to levelables

core.js changes:
    async f: change stage
        stage_id
        // validates the stage id
        // changes content_area HTML from template


world class:
    this.jobs
    this.skills
    this.items

    this.resources
    this.active
    this.skill_effects

    constructor
        categories, jobs_data, skills_data, items_data
        // arg notes: switch to generating all this and instead taking meta-value parameters once procedural gen
        // master-skill notes: generate a copy of master skills which include the main master-skills but also add categorical ones
        // temp: hook the handmade data into this
    f: setup html
    f: Time()
        value
        // passes to Time.format()
    f: Currency()
        value
        // passes to Currency.format()
    f: Jobs
        // returns jobs list
    f: skills
        // returns skills list
    f: items
        // returns items list
    f: Tick()
        deltams
        // hooks into main Gameloop function and executes all world-stage actions
    destructor
        // stops the game loop function